import "github.com/gox-lang/gox/gui"

fx func Counter() {
    let count = 0
    
    return <button text="Click" onClick={() => {
        count = count + 1
    }} />
}

func Main() {
    let counter = NewCounter()
    println("FX Component Test")
}