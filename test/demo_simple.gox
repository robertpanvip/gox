package main

import "github.com/gox-lang/gox/gui"

func Main() {
    let app = gui.NewApp("Demo", 800, 600)
    
    let title = <Label text="Hello TSX!" size={24} />
    app.Root.AddChild(title)
    
    let btn = <Button text="Click" />
    app.Root.AddChild(btn)
}
